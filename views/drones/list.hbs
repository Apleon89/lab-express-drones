<div id="dron-list-container">
  <h2>Available drones</h2>
  <div id="dron-container">
    {{#each allDrones}}
      <div class="each-dron-container">
      <h4>Name: {{this.name}}</h4>
      <p>Propellers: {{this.propellers}}</p>
      <p>Speed: {{this.maxSpeed}}</p>
      <a href="/drones/{{this._id}}/edit"><button>Actualizar data del Dron</button></a>
      <form action="/drones/{{this._id}}/delete" method="post">
      <button>Borrar Drone</button>
      </form>
      </div>
    {{/each}}
  </div>
</div>